#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createReviewMasterInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createReviewMasterInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO ReviewMaster $colStr VALUES $valStr", "SELECT * FROM ReviewMaster WHERE id=LAST_INSERT_ID()"]
}