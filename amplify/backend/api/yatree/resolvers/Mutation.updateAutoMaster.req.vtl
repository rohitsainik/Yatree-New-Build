#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateAutoMasterInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateAutoMasterInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE AutoMaster SET $update WHERE id=$ctx.args.updateAutoMasterInput.id", "SELECT * FROM AutoMaster WHERE id=$ctx.args.updateAutoMasterInput.id"]
}