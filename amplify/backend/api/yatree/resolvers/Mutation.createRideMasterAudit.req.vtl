#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createRideMasterAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createRideMasterAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO RideMasterAudit $colStr VALUES $valStr", "SELECT * FROM RideMasterAudit WHERE auditId=$ctx.args.createRideMasterAuditInput.auditId"]
}