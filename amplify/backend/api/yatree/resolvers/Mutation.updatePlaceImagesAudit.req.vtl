#set( $updateList = {} )
#foreach( $entry in $ctx.args.updatePlaceImagesAuditInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updatePlaceImagesAuditInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE PlaceImagesAudit SET $update WHERE auditId=$ctx.args.updatePlaceImagesAuditInput.auditId}", "SELECT * FROM PlaceImagesAudit WHERE auditId=$ctx.args.updatePlaceImagesAuditInput.auditId"]
}