#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createPackageMasterAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createPackageMasterAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO PackageMasterAudit $colStr VALUES $valStr", "SELECT * FROM PackageMasterAudit WHERE auditId=$ctx.args.createPackageMasterAuditInput.auditId"]
}