#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateRideMasterInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateRideMasterInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE RideMaster SET $update WHERE id=$ctx.args.updateRideMasterInput.id", "SELECT * FROM RideMaster WHERE id=$ctx.args.updateRideMasterInput.id"]
}