#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createOfferMasterAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createOfferMasterAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO OfferMasterAudit $colStr VALUES $valStr", "SELECT * FROM OfferMasterAudit WHERE auditId=$ctx.args.createOfferMasterAuditInput.auditId"]
}