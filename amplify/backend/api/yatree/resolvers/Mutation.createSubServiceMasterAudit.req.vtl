#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createSubServiceMasterAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createSubServiceMasterAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO SubServiceMasterAudit $colStr VALUES $valStr", "SELECT * FROM SubServiceMasterAudit WHERE auditId=$ctx.args.createSubServiceMasterAuditInput.auditId"]
}