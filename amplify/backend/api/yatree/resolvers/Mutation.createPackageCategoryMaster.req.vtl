#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createPackageCategoryMasterInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createPackageCategoryMasterInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO PackageCategoryMaster $colStr VALUES $valStr", "SELECT * FROM PackageCategoryMaster WHERE id=LAST_INSERT_ID()"]
}