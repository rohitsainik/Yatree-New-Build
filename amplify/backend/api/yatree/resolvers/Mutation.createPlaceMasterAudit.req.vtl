#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createPlaceMasterAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createPlaceMasterAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO PlaceMasterAudit $colStr VALUES $valStr", "SELECT * FROM PlaceMasterAudit WHERE auditId=$ctx.args.createPlaceMasterAuditInput.auditId"]
}