#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateApplicationMasterInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateApplicationMasterInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE ApplicationMaster SET $update WHERE id=$ctx.args.updateApplicationMasterInput.id", "SELECT * FROM ApplicationMaster WHERE id=$ctx.args.updateApplicationMasterInput.id"]
}