#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createReviewMasterAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createReviewMasterAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO ReviewMasterAudit $colStr VALUES $valStr", "SELECT * FROM ReviewMasterAudit WHERE auditId=$ctx.args.createReviewMasterAuditInput.auditId"]
}