#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createBookingPassengerDetailsAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createBookingPassengerDetailsAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO BookingPassengerDetailsAudit $colStr VALUES $valStr", "SELECT * FROM BookingPassengerDetailsAudit WHERE auditId=$ctx.args.createBookingPassengerDetailsAuditInput.auditId"]
}