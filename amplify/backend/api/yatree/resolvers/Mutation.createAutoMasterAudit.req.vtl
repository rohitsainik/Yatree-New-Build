#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createAutoMasterAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createAutoMasterAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO AutoMasterAudit $colStr VALUES $valStr", "SELECT * FROM AutoMasterAudit WHERE auditId=$ctx.args.createAutoMasterAuditInput.auditId"]
}