#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createSubServiceMasterInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createSubServiceMasterInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO SubServiceMaster $colStr VALUES $valStr", "SELECT * FROM SubServiceMaster WHERE id=LAST_INSERT_ID()"]
}