#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createDiscountMasterAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createDiscountMasterAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO DiscountMasterAudit $colStr VALUES $valStr", "SELECT * FROM DiscountMasterAudit WHERE auditId=$ctx.args.createDiscountMasterAuditInput.auditId"]
}