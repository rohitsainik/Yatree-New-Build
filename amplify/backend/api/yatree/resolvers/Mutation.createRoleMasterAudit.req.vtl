#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createRoleMasterAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createRoleMasterAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO RoleMasterAudit $colStr VALUES $valStr", "SELECT * FROM RoleMasterAudit WHERE auditId=$ctx.args.createRoleMasterAuditInput.auditId"]
}