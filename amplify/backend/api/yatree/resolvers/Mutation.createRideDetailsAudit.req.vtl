#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createRideDetailsAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createRideDetailsAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO RideDetailsAudit $colStr VALUES $valStr", "SELECT * FROM RideDetailsAudit WHERE auditId=$ctx.args.createRideDetailsAuditInput.auditId"]
}