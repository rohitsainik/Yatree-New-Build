#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createSupportMasterAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createSupportMasterAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO SupportMasterAudit $colStr VALUES $valStr", "SELECT * FROM SupportMasterAudit WHERE auditId=$ctx.args.createSupportMasterAuditInput.auditId"]
}