#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createPackagePlaceMappingAuditInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createPackagePlaceMappingAuditInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO PackagePlaceMappingAudit $colStr VALUES $valStr", "SELECT * FROM PackagePlaceMappingAudit WHERE auditId=$ctx.args.createPackagePlaceMappingAuditInput.auditId"]
}